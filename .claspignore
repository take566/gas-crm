# Clasp ignore file
# ここに記載したファイル/ディレクトリは clasp push でアップロードされません。
#
# .clasp.json の rootDir は "src" なので、src/ の外（docs/, node_modules/,
# package.json, README.md など）はそもそも push 対象になりません。
# したがってここでは src/ 配下に置かれうる非 GAS ファイルだけを除外します。

# ドキュメント
*.md

# 一時ファイル
*.tmp
*.log
